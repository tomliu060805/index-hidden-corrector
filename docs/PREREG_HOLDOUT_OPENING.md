# 预注册 · holdout 一次性开封（写在任何数字之前）

2026-09-22。用户指令开封。**这是本项目最后一段未读数据，读完这条线只能前向累计。**

## 开什么
`holdout = 2025-07-18 ~ 2026-09-16`，285 个交易日（比 test 的 269 天更长，功效更足）。

## 配置：与 test 开封**完全一致**，不扫参、不选优
5min×L=128 输入 / `L8n`-last 池化 / H=6（30min）/ 基线 **B8+B9** / 140 策展指数。
拟合只用 **train**（≤2023-04-27），λ 只在 **val**（≤2024-06-07）上选。
**test 不参与拟合也不参与选参**（它已于 2026-09-17 开封，此处仅作对照报告）。

## 主判据（一条）
> **holdout 段 ΔR² > 0，且 CW t > 3。**

不成立 ⇒「v2 的预报层增量不能外推」，按判负库格式入库，这条线结束。

## 预先写下的**预期值**（防事后合理化）
已知：train +0.0171 / val +0.0148 / **test +0.0127**（test/val 比 0.86）。

- **holdout ΔR² ≥ +0.008** ⇒ 与 test 一致，无进一步衰减，结论完全确立
- **holdout ΔR² ∈ (0, +0.008)** ⇒ 继续衰减，成立但幅度按 holdout 报
- **holdout ΔR² ≤ 0** ⇒ 判负

## 同时报告、但**不作判据**的量
事先列出，防止开封后挑一个好看的当结论：
- **QLIKE**：test 上是 **−14.8%**（负）。**预期 holdout 同样为负。**
  若为正，也**不**据此恢复任何风控主张 —— 单段翻正不足以推翻 test 的负号。
- 逐指数为正比例、中位、p10
- 四个主力宽基（上证50/沪深300/中证500/中证1000）
- 极端波动分位桶的梯度
- 目标 C（子采样 5min RV，无偏+低噪）下的同一组数 —— **A 仍是头条**，C 只作稳健性

## 变体计数
1 个配置 × 1 个视界 × 2 个目标构造（A 头条 / C 稳健性）= 2 格。无其他搜索。

## 执行后
① 全部数字写进 `RESULTS_v2.md`；② 入判负库；③ **此后 holdout 不得再用于任何调参**，
本项目转为前向累计（forward-only）。
