# v2 结论：冻结时序基座隐层对指数日内已实现波动的预报增量

**定位：预报层。** 本文只回答一个问题 —— 在信息集对齐的最强 HAR 基线之上，
冻结的 Kronos-small 隐层对宽基指数日内已实现波动还有没有增量。
不涉及交易、仓位或风控落地。

预注册 `docs/PREREG_v2_layerscan.md`（写在任何数字之前）、
`docs/PREREG_TEST_OPENING.md`（写在读 test 之前）。

---

## 0. 一句话结论

**有，且外推到样本外。**

| 段 | 基线 R² | +隐层 R² | **ΔR²** | **CW t** | 逐指数为正 |
|---|---|---|---|---|---|
| train | 0.6439 | 0.6609 | +0.0171 | 22.90 | — |
| val | 0.4929 | 0.5077 | +0.0148 | 11.06 | — |
| **TEST**（一次性开封） | 0.5894 | 0.6021 | **+0.0127** | **10.34** | **140/140** |

`test` = 2024-06-11 ~ 2025-07-17，269 天，22.6 万决策点。
**test/val 增量比 0.86**，落在预注册写死的「无衰减」带 `[+0.010, +0.020]` 内。

配置：5min×L=128 输入 / `L8n`-last / H=6（30min）/ 基线 B8+B9 / 140 个策展指数。
拟合只用 train，λ 只在 val 上选，**test 只用于评估**。

`holdout`（2025-07-18 之后）**仍然封存**。

### 广度

```
逐指数为正   140/140 (100%)      中位 +0.0152      p10 +0.0093（仍为正）
上证50 +0.0139 | 沪深300 +0.0131 | 中证500 +0.0224 | 中证1000 +0.0168
```

**p10 为正** —— 连第 10 百分位的指数都有正增量，不是被少数带动。

### 增量在极端波动时最大（test 上同样成立）

| 分位桶（PIT 同 bar 分位） | ΔR² | MSE 下降 |
|---|---|---|
| 0-50% | +0.0173 | 2.9% |
| 90-95% | +0.0223 | 4.0% |
| **99-100%** | **+0.0310** | **7.1%** |

### ⚠️ 明确不成立的：QLIKE 口径

| 段 | QLIKE 改善 | DM t |
|---|---|---|
| val | +1.4% | 1.61 |
| **TEST** | **−14.8%** | −1.01 |

两段都未建立，点估计一正一负。test 上「基线低估」那一侧的 QLIKE 是 **−24.6%**，
严重低估的前 10% 是 **−26.1%**。

**所以本文的主张严格限于对称 log 误差（R²/MSE）口径：
模型能更准地预测波动的平均水平，不能用来避免低估波动。**

---

## 0. 口径变更与它为什么不重要

项目 v1 的目标与特征都是**平均绝对收益 MAD**（`mean|r|`），不是已实现波动。
v2 换成文献标准的 RV。实测两者在对数空间几乎是同一个东西：

| H | corr(log RV, log MAD) | std 比值 | 峰度 logRV / logMAD |
|---|---|---|---|
| 30min | 0.9881 | 1.98 | 0.65 / 0.59 |
| 1h | 0.9902 | 1.99 | 0.76 / 0.67 |
| 2h | 0.9916 | **2.00** | 0.77 / 0.60 |

std 比值恰好 2.00 是代数结果：`log RV ≈ 2·log MAD + 常数`。
**对数变换消掉了 RV 的厚尾问题**——原始收益峰度 20.5 / 25.4 / 32.6，取对数后 log RV 只有 0.76。
这正是文献用 log-HAR 而非 level HAR 的原因。

实证确认：同一套基线阶梯在 H=12 上，MAD 口径 B4=0.5616 / RV 口径 B4=0.5582，
**各级差 +0.003，排序完全相同**。换目标不改变任何结论；真正该换的是基线。

*v1 报告的 ΔR²+0.095 是 MAD 口径且基线更弱，与本文数字不可直接比较。*

## 1. 基线阶梯：B\* = HAR-RS，三视界全胜

| id | 规格 | H=30min | H=1h | H=2h |
|---|---|---|---|---|
| **B4** | **HAR-RS（上下行半方差）** | **0.4682** | **0.5582** | **0.6420** |
| B5 | B2 + EWMA/RiskMetrics | 0.4572 | 0.5473 | 0.6314 |
| B2 | HAR + 日内节律 + 指数独热 | 0.4562 | 0.5459 | 0.6300 |
| B3 | HARQ（√RQ 交互） | 0.4549 | 0.5442 | 0.6299 |
| B1 | HAR-RV（Corsi 2009 三尺度） | 0.4124 | 0.5216 | 0.6099 |
| B0 | 零拟合 RW（无参数） | 0.0642 | 0.2637 | 0.4551 |

- **B4 三视界全胜**，比 B2 稳定高 +0.012。三次同号，不是自由度效应——B3 同样多 2 列却更差。
- **★ HARQ 在日内频率上无效**：三视界都略输 B2（−0.0013 / −0.0017 / −0.0001）。
  Bollerslev-Patton-Quaedvlieg 的 √RQ 修正是为**日频** RV 的测量误差推导的；
  5min bar × 12 根窗口上没有兑现。
- 判负库里 `LM-GARCH` 那条的教训（「波动率模型的复杂度几乎从不赢过 HAR-RV」）在这里只成立一半：
  HAR-RV 本身不是最强的，半方差分解稳定地更好。

## 1.5 七视界扫描：30min 是主视界

`H ∈ {1,2,3,4,6,12,24}` 根 5min bar = 5/10/15/20/30/60/120 分钟。
增量口径 = 残差 + 岭（确定性，无种子），相对各自的 B\*。

| 视界 | 基线 B\* | 绝对 ΔR² | 相对 Δ/B\* | **bar 级占比** | 功效探针 |
|---|---|---|---|---|---|
| 5min | 0.1024 | +0.0087 | 8.5% | **93%** | +0.0004 ⚠️ |
| 10min | 0.2267 | +0.0202 | **8.9%** | 90% | +0.0010 ⚠️ |
| 15min | 0.3277 | +0.0265 | 8.1% | 85% | +0.0035 |
| 20min | 0.3908 | +0.0294 | 7.5% | 83% | +0.0065 |
| **30min** | 0.4682 | **+0.0345** | 7.4% | 80% | +0.0114 |
| 60min | 0.5582 | +0.0248 | 4.4% | 64% | +0.0112 |
| 120min | 0.6420 | +0.0187 | 2.9% | **27%** | +0.0138 |

- **绝对增量在 30min 达峰**；**相对增量在 5–30min 基本持平（7.4~8.9%），60min 起断崖**
- **bar 级占比单调递减 93% → 27%**

### ★ 5min 上隐层比整个经典短尺度块强 4 倍

⚠️ 5/10min 的功效探针几乎为零（+0.0004 / +0.0010），**不是管线没功效，是探针本身没东西**：
B\* = 0.1024，抽掉整个 12 根尺度后的弱基线是 0.1003 —— 经典的 1 小时已实现波动块
在 5min 视界上只值 **+0.002**，而隐层给 **+0.0087**，是它的 **4 倍多**。
（功效仍有保障：安慰剂塌到 −0.0002，信号是它的 40 倍，n=36.9 万。
但按项目规矩不能说「功效已验证」，只能说「这个探针在短视界上不适用」。）

**主视界定为 30min**：绝对增量最大、bar 级占比仍有 80%、基线 R² 0.47 是可信的水平
（5min 只有 0.10 —— 目标本身几乎测不准，增量再漂亮也撑不起「预报器」这个定位）。

## 2. 主判据：换成最强基线后，隐层增量仍然成立

预注册的 confirmatory 判据是「H=12、L8n/last、残差+GatedLinear，val ΔR² > 0 且 5 种子全正」。

**成立**：Δ = **+0.0276 ± 0.0014**，5/5 种子为正。

三视界全表（相对 B\*）：

| 方法 | H=30min | H=1h | H=2h |
|---|---|---|---|
| **T1 联合回归（岭）** | **+0.0461** | **+0.0368** | **+0.0281** |
| T2 残差 + GatedLinear（5 种子） | +0.0419 ±0.0012 | +0.0276 ±0.0014 | +0.0204 ±0.0016 |
| T2r 残差 + 岭 | +0.0345 | +0.0248 | +0.0187 |
| （隐层单独预测目标） | 0.1242 | 0.0953 | 0.0883 |
| **T3 等权集成（½B\*+½隐层）** | **−0.0545** | **−0.0794** | **−0.0950** |

### ★ 2.1 等权集成判负，且根因清楚

[arXiv 2607.05291](https://arxiv.org/abs/2607.05291) 报告 TTM 与 Log-HAR 的**等权集成**胜过任一单独，
这是「为什么用残差学习而不用简单集成」的现成对照。

**在这里等权集成三视界全负，且越长视界越糟。** 根因在「隐层单独预测」那一行：
隐层自己只有 R² 0.088~0.124，基线是 0.468~0.642，**差 4~6 倍**。
把强预测器和弱得多的预测器平均，必然被拖下去。

**分界线是强度是否相当**：2607.05291 里 TTM ≈ Log-HAR，集成才有意义；
这里隐层是**补充品不是替代品**，只能以残差/联合的方式进入。

### ★ 2.2 联合回归稳定优于残差修正

T1 在三视界都高出 T2 约 +0.004~0.009。说明隐层的价值不止是「加一个正交增量」，
还包括**条件于隐层重新加权 HAR 的三个尺度**。现行生产用的是残差形态，这是一个改进方向。

### 2.3 增量随视界单调递减 —— 与 v1 结论相反

+0.0461 → +0.0368 → +0.0281（T1 口径）。v1 报的是「1h 是峰」的倒 U 曲线。
差别来自基线：HAR-RS 在长视界上强得多（0.468→0.642），**剩给模型的就少了**。
换句话说，v1 的倒 U 峰有一部分是弱基线在长视界上的失效造成的假象。

## 3. 逐层扫描：不换层，但深度效应真实

Kronos-small 有 8 个 Transformer block。扫描 10 个位置 × 2 种池化 × 3 视界 = **60 格**
（候选数在预注册里写死）。残差口径 ΔR²：

| 位置 | H=6 | H=12 | H=24 | 三视界名次 |
|---|---|---|---|---|
| L0（embedding） | 0.0133 | 0.0160 | 0.0181 | 10/9/6 |
| L1 | 0.0139 | 0.0147 | 0.0161 | 9/10/8 |
| L2 | 0.0200 | 0.0176 | 0.0162 | 8/8/7 |
| L3 | 0.0263 | 0.0203 | 0.0161 | 7/7/9 |
| L4 | 0.0328 | 0.0233 | 0.0157 | 6/6/10 |
| L5 | 0.0356 | **0.0272** | 0.0182 | 2/1/5 |
| L6 | **0.0357** | 0.0263 | 0.0186 | 1/2/4 |
| L7 | 0.0346 | 0.0253 | **0.0192** | 4/4/1 |
| L8 | 0.0346 | 0.0256 | 0.0191 | 3/3/2 |
| **L8n（现行生产）** | 0.0345 | 0.0248 | 0.0187 | 5/5/3 |

**按预注册判据①（必须三视界都进前 2）：L5 是 2/1/5，L6 是 1/2/4，都不满足 ⇒ 不换层。**
最优层相对现行的优势只有 +0.0012 / +0.0024 / +0.0005。

两个真实发现：

- **深度效应大**：L0→L5 在 H=6 上从 0.0133 涨到 0.0356（**2.7 倍**），之后持平。
  与 [arXiv 2511.15324](https://arxiv.org/abs/2511.15324)「深层编码 dispersion」一致。
- **★ TimeRep 的「最后一层最差」在这里不复现**。L8n 排 5/5/3，中游，离最优仅差 0.002。
  可能的原因：[TimeRep](https://arxiv.org/html/2509.12650v1) 用的 MOMENT-Large 是 24 层
  **encoder-only + 掩码重建**；Kronos-small 是 8 层 **decoder-only 自回归**，
  深度可能不足以出现「末层过拟合预训练目标」。

### ★ 3.1 池化：`last` 完胜 `mean`（2.6 倍）

均值 0.0260 vs 0.0100。encoder 类 TSFM 的线性探针惯例是 mean-pooling（Chronos、MOMENT 皆然），
但**在因果 decoder 上不适用**：第 k 个位置的隐状态只看见第 1~k 根 bar，
平均等于把大量「只看了窗口前半段」的陈旧状态混进来。
这条把候选维度从 20 砍到 10。

## 4. 零基准：四项全过，并量出「日级 vs bar 级」的拆分

| 检验 | H=30min | H=1h | H=2h | 判读 |
|---|---|---|---|---|
| 隐层 512 维（真值） | +0.0345 | +0.0248 | +0.0187 | 参照 |
| **[S6a] 按天整体打乱** | −0.0005 | +0.0000 | +0.0002 | ✓ 三视界全塌到零 |
| **[S6c] 功效正对照** | +0.0114 | +0.0112 | +0.0138 | ✓ 管线有功效 |
| **[M4] 只用 nll+ent 两标量** | — | +0.0008 | +0.0000 | ✓ 只拿到 512 维的 3% |
| [M3] 基线扩到 7 个 RV 尺度 | +0.0331 | +0.0247 | +0.0201 | ✓ 不是「更多尺度」换名字 |

### ★★ 4.1 增量里有多少是 bar 级的

把决策点的隐层换成**当日第一根 bar（09:35）**的隐层 —— 严格早于当日全部决策点，无前视。
剩下的就是「日级、且当天开盘即可得」的成分；差额才是真正的 bar 级信息。

| H | 真值 | S6e（当日首根，严格因果） | 日级占比 | **bar 级占比** |
|---|---|---|---|---|
| **30min** | +0.0345 | +0.0068 | 20% | **80%** |
| 1h | +0.0248 | +0.0090 | 36% | 64% |
| 2h | +0.0187 | +0.0136 | 73% | **27%** |

**bar 级成分随视界单调衰减。30min 上 80% 是真正的日内时序信息，2h 上只剩 27%。**

对 2h 的实用推论：73% 的增量在当天 09:35 就能拿到，
即**每天 1 次前向**可得四分之三的价值（省 36~48 倍算力）。但更好的选择是换到 30min。

### ⚠️ 4.2 一个必须记下的检验设计错误（两处，都会给出完全相反的读数）

1. **功效正对照第一版无效**：只抽掉 `rv12` 却保留了 `rs_up`/`rs_dn`，而这两者是**同一个
   12 根窗口**的上下行半方差，`rs_up + rs_dn ≈ rv12` ⇒ 等于什么都没抽掉，
   正对照必然给 0，会被误读成「管线没功效」。改为抽掉整个 12 根尺度后给 +0.011~0.014。
2. **★日内打乱含前视**：把同一天内的隐层随机换位置，会取到**当天更晚时刻**的隐层
   —— 那是未来信息。该口径下 1h/2h 的「日级占比」分别是 101% 和 137%，
   会得出「长视界增量全是日级」的错误结论。改成只用当日首根（S6e）后，
   真实的日级占比是 36% 和 73%。**趋势是真的，程度差了一倍。**

## 4.5 统计显著性（H=6，val 段，`src/dm_test_v2.py`）

| | B\* (HAR-RS) | +隐层 | Δ |
|---|---|---|---|
| R² | 0.4682 | 0.5027 | **+0.0345** |
| **Clark-West t** | | | **13.06** |
| DM t（参照） | | | 6.04 |
| QLIKE 改善 | | | **6.9%**（DM t=3.75） |
| n（天） | | | 268 |

**以 CW 为准**：B4 与 B4+隐层是**嵌套**的，DM 在嵌套下有偏（原假设下大模型的多余参数
只带来估计噪声，损失差均值天然为负，系统性低估）。Clark-West 修正了这项偏差。

QLIKE 口径独立确认（+6.9%）—— 这条重要，因为 Patton 2011 证明 QLIKE 对波动代理的
噪声稳健，而 R² 不是。

*注：train 段的 QLIKE DM t 只有 1.12，低于 val 的 3.75。根因是 QLIKE 的乘性偏差校正
`c` 在 train 上算，把 train 两模型的 QLIKE 拉平了。不影响 val 结论。*

## 5. 144 个指数上的复查：97% 为正，唯一的失败是 B 股

universe 由 `src/screen_indices.py` 机械筛出 480 个，再按「地域+主题全砍 / 宽基+风格全留 /
行业只留一族（深证一级 10 个）/ 去重 corr>0.99」策展到 **144 个**（`src/curate_universe.py`）。
隐层提取 306 万决策点（stride 6），评估 262 万点（H=6）。

| | H=30min | H=1h | H=2h |
|---|---|---|---|
| 池化 ΔR² | +0.0260 | +0.0179 | +0.0200 |
| **逐指数为正** | **140/144 (97.2%)** | 141/144 (97.9%) | 140/144 (97.2%) |
| 中位 Δ | **+0.0309** | +0.0218 | +0.0258 |
| p10 / p90 | **+0.0167** / +0.0429 | +0.0119 / +0.0311 | +0.0082 / +0.0391 |

**p10 为正** —— 连第 10 百分位的指数增量都是正的，不是被少数指数带动。
原来那 3 个指数不特殊：3 指数口径 +0.0345 vs 144 指数中位 +0.0309，同一量级。

### ★★ 5.1 H=30min 上是完美分离：失败只发生在 B 股

144 个里**恰好有 4 个 B 股指数**（B股指数 / 成份B指 / 深证B指 / 巨潮B股指数），
**4/4 为负**；其余 **140 个全为正**，最小 +0.0014，中位 +0.0311。

B 股是结构性隔离的市场（外币计价、投资者群体不同、成交极稀），
Kronos 的预训练语料（45 个交易所的 K 线）里大概率没有同类。
**这是个可解释且封闭的失败模式，不是随机失败。**
（H=12/24 上分离没这么干净：B 股 1/4 为负，非 B 股另有 2~3 个为负。）

### 5.2 按指数类型：行业指数上最强

| 类型 | n | 均值 Δ (H=6) | 为正 |
|---|---|---|---|
| **行业（深证一级）** | 10 | **+0.0343** | **100%** |
| 风格 | 86 | +0.0300 | 99% |
| 宽基 | 48 | +0.0289 | 94% |

三个视界上行业都最高。宽基为正比例最低（94%）正是因为 4 个 B 股指数都被归在宽基里。

### 5.3 「基线越难增量越大」——在干净口径下成立（一次自我更正）

一度报「在截面上不复现」，那是**脏口径**。隔离后：

| 设置 | `corr(基线R², 增量)` Spearman |
|---|---|
| B4 + 含 B 股（144 个） | −0.029 |
| B4 + 去 B 股（140 个） | −0.115 |
| **B6 + 去 B 股（140 个）** | **−0.247** |

B 股是「低基线 R² **且** 负增量」的离群点，会把相关拉向零；弱基线又进一步稀释。
**两层都清掉之后，v1 那条结论成立。**

★教训：报「某关系不复现」之前，先把已知的结构性离群点剔掉并换上最强基线 ——
否则报的是**离群点的稀释效应**，不是关系本身。

### 5.4 universe 的独立性：不能说「144 次独立验证」

480 个指数的**参与率有效独立维数只有 3.1**（名义 480），14 个主成分解释 90% 方差；
63 对相关 >0.995 是字面重复（沪深双挂 / `R` 全收益版 / 上证指数=A股指数=新综指）。
策展去重后仍远小于名义个数。

**所以正确的陈述是广度而非独立性**：增量在 A 股各类指数（宽基 / 风格 / 行业）上**普遍成立**，
而不是获得了 144 次独立确认。

## 5.5 最强基线下的定论（B6，去 B 股）

用户指出：增量只有在「基线里该有的东西全加进去」之后才有意义。于是把基线继续加强：

| 基线 | 规格 | val R² (H=6) |
|---|---|---|
| B4 | HAR-RS | 0.4682 |
| **B6** | **B4 + 过去 12 根带符号收益（原始路径）** | **0.4698** |
| B7 | B6 + 逐根平方 | 0.4697 |

滞后收益值 **+0.0016**；**逐根平方白加**（B7−B6 ≈ 0）—— 说明滞后收益带进来的是**符号/方向**，
二阶信息早已在 RV 里。

**关键：更强的基线没有吃掉隐层的增量，反而略微放大。**

| 基线 | 基线 R² | +隐层 | ΔR² | CW t |
|---|---|---|---|---|
| B4 | 0.4682 | 0.5027 | +0.0345 | 13.06 |
| **B6** | 0.4698 | 0.5050 | **+0.0353** | **13.43** |
| B7 | 0.4697 | 0.5050 | +0.0352 | 13.47 |

这是个有信息量的负面排除：**隐层带的不是「最近几根的路径形状」** —— 那正是滞后收益捕捉的东西。

### ★ 140 个非 B 股指数 × 最强基线 B6：140/140 全为正

| | |
|---|---|
| 池化 | B\* 0.4207 → 0.4494，**Δ +0.0287** |
| **逐指数为正** | **140/140（100%）** |
| 中位 / p10 / p90 | **+0.0320** / **+0.0209** / +0.0437 |
| 最差的那一个 | **+0.0026**（仍为正） |
| 行业 / 宽基 / 风格 | +0.0362 / +0.0330 / +0.0315，**三类全 100% 为正** |

*注：144 指数面板的池化 B\*（0.42）低于 3 指数面板（0.47），是因为面板异质性大得多 ——
3 指数的指数独热只区分大中小盘，144 指数的 140 个哑变量覆盖宽基/风格/行业。*

## 5.6 ★★增量集中在极端波动时段（用户 2026-09-17 提出并证实）

条件变量：当前 `rv12`（过去 1 小时已实现波动）在**过去 252 个交易日、同一 bar**
的分布里的滚动分位 —— 严格 PIT。

**按 bar 分组这一步不能省**：日内节律让 09:40 的波动天生是 15:00 的 3.4 倍，
不分组会把「现在是早盘」误判成「现在很极端」。

140 个非 B 股指数，H=6，基线 B6：

| 分位桶 | n | 基线 R² | ΔR² | 基线 MSE | **MSE 下降** | 安慰剂 ΔR² | **安慰剂 MSE 下降** |
|---|---|---|---|---|---|---|---|
| 0-50% | 111,833 | 0.1943 | +0.0254 | 0.655 | **3.2%** | −0.0028 | −0.4% |
| 50-70% | 43,044 | 0.2125 | +0.0360 | 0.598 | 4.6% | −0.0015 | −0.2% |
| 70-80% | 20,817 | 0.2407 | +0.0383 | 0.597 | 5.0% | +0.0011 | 0.1% |
| 80-90% | 21,960 | 0.2600 | +0.0405 | 0.617 | 5.5% | +0.0033 | 0.5% |
| 90-95% | 11,530 | 0.2581 | +0.0494 | 0.667 | 6.7% | +0.0055 | 0.7% |
| **95-99%** | 10,819 | 0.2323 | **+0.0853** | 0.713 | **11.1%** | +0.0047 | 0.6% |
| **99-100%** | 5,117 | 0.3648 | **+0.1409** | 0.836 | **22.2%** | +0.0057 | **0.9%** |

**ΔR² 5.5 倍，MSE 下降 6.9 倍，完美单调。** H=12 / H=24 顶桶几乎相同
（+0.1127 / 20.0% 与 +0.1119 / 21.3%）。

### 安慰剂：模式是真的，但有约 4% 的机械成分

按天整体打乱隐层后同样分桶，**安慰剂确实有轻微上升趋势**（−0.0028 → +0.0057），
所以分桶本身有机械效应。但量级不是一回事：

- 安慰剂全程跨度 **1.3pp**（−0.4% → 0.9%）；真信号 **19pp**（3.2% → 22.2%）
- 顶桶里机械成分占 **0.9 / 22.2 ≈ 4%**，真信号是安慰剂的 **25 倍**

### ★ 这不是「在已经好的地方更好」，是「救火」

3 指数版一度显示基线 R² 随分位单调上升（0.16→0.47），据此曾读成「两者都变准、
模型变得更快」。**140 指数版推翻了这个读法**：

- 基线 **MSE 单调上升** 0.655 → 0.836（绝对误差在极端尾部明显变大）
- 基线 **R² 非单调**，在 95-99% 那桶反而掉到 0.2323

**极端尾部里基线是在变差的**，隐层从一个更大的误差里拿掉了更大的比例。
对风控用途而言，这比原先的读法值钱得多。

### 与另外两条观察合流

| 来源 | 观察 |
|---|---|
| v1（3 指数时序） | 「基线越难的 regime 增量越大」 |
| v2（140 指数截面） | `corr(基线R², 增量) = −0.247` |
| **本节（时序分位）** | **99-100% 桶 MSE 下降 22.2%，是底桶 3.2% 的 6.9 倍** |

三者指向同一件事：**隐层的增量集中在经典模型最吃力的地方** ——
截面上是「难预测的指数」，时序上是「极端波动时段」。

## 5.7 ★★★ B8：信息集对齐之后，结论必须改写

**问题**：Kronos 的输入是 `[open, high, low, close, volume, amount]` **六列**，
而原基线只用了 **close 一列**。这个不对称让「增量」一词混了两件事 ——
① 表示的价值（同一份信息里提炼出经典特征提炼不出的东西）
② **信息的价值**（模型看得见而基线根本没看的东西）。只有 ① 是本项目想说的。

**B8 = B6 + 成交额8尺度 + 日内累计RV(不跨日) + 隔夜跳空 + 跳跃分解(bipower)
+ Parkinson/GK 高低价估计量 + 日历（星期/月初末）**，共 32 项新特征。

### 基线本身大幅变强

| 基线 | val R² (H=6, 3指数) | QLIKE |
|---|---|---|
| B6 | 0.4698 | 0.3442 |
| **B8** | **0.4930** (+0.0232) | **0.2982** |

B8 的增益是滞后收益（+0.0016）的 **14 倍**。

### 140 指数上的显著性（★所有显著性一律在此面板判）

DM/CW 先按**天**聚合再做 HAC，故 n 是天数（val 268 天），
140 个指数不通过横截面伪重复吹大 t 值，只是把每天的损失差估得更准。

| 基线 | ΔR² | DM t | **CW t** | **QLIKE 改善** | **QLIKE DM t** | 逐日为正 |
|---|---|---|---|---|---|---|
| B6 | +0.0287 | 5.24 | **11.94** | +4.2% | **2.96** ✓ | 56.3% (z=+2.08) |
| **B8** | +0.0258 | 6.36 | **12.17** | **+1.1%** | **0.91** ✗ | **49.6% (z=−0.12)** |

**B8 下 QLIKE 逐日为正只有 49.6% —— 抛硬币。** 非离群点所致
（winsor 1% 后 t 仍只有 1.00，中位数 −0.00015 微负）。

### ★★★ 分侧诊断：价值全在「高估侧」，低估侧是负的

| 情形 | 占比 | 基线 MSE | MSE 下降 | QLIKE 下降 |
|---|---|---|---|---|
| **B6** 基线低估 | 49.5% | 0.6081 | +2.4% | +3.3% |
| **B6** 基线高估 | 50.5% | 0.6760 | +7.2% | +6.3% |
| **B6** ★严重低估(前10%) | 10% | 2.0170 | **+8.5%** | **+8.7%** |
| **B8** 基线低估 | 46.6% | 0.5211 | **−5.4%** | **−5.0%** |
| **B8** 基线高估 | 53.4% | 0.6523 | **+12.0%** | +11.1% |
| **B8** ★严重低估(前10%) | 10% | 1.6911 | **+1.2%** | +0.8% |

> **在信息集对齐的最强基线下，隐层的全部价值来自「基线高估波动时把它拉下来」（+12.0%）；
> 在「基线低估波动」时它把预测推得更错（−5.4%）。** 净增量为正只因高估情形略多（53.4%）
> 且基数误差更大。

**对风控而言这是坏消息**：波动预测最危险的失败是**低估**（以为平静、实际要炸）。
隐层在那一侧不仅没帮忙，还是负的；严重低估的前 10% 里只剩 +1.2%（B6 下是 +8.5%）。

这也解释了 QLIKE 为什么失效 —— QLIKE 正是专门对低估罚重的损失函数。

### ⚠️ 5.7.x 上面这段「风控用途不成立」已被 §5.8 撤回

B8 补齐了 Kronos 看得见的**六列**，但**没补齐同样六列在 1min 频率上的派生量**（B9）。
B8 本身是**系统性高估**的（53.4% 的点被高估），而一个偏高的基线会让任何
「往下修正」的东西看起来一边倒地有用。详见 §5.8。

★ 但这一节的教训不变、而且更重要：**若不补 B8，本项目会拿着
「ΔV +0.0353 / CW 13.43 / QLIKE +7.0%」收工**，那个数字里混着「基线没看见的信息」。

## 5.8 ★★★ B9（1min 派生的基线特征）：结论再次反转，风控用途成立

**B9** = 从 **1 分钟** 数据派生的基线特征（`src/har_1m_features.py`，19 项）：
1min RV 三尺度 / **子采样平均 RV**（降微观结构噪声的标准做法）/ 1min Parkinson 三尺度 /
1min 跳跃分解（bipower）+ 已实现四次方差 / **5min bar 内成交额的分布形状**（5min 输入看不见）。

同时放裸 1min RV 与子采样 RV，是因为
[Liu-Patton-Sheppard](https://doi.org/10.1016/j.jeconom.2015.02.008) 证明 1min RV 被
微观结构噪声污染、5min 才是甜点 —— 只加裸 1min RV 会把基线做坏，那是另一种信息集不对等。

### 140 指数 val（★显著性以此面板为准）

| | B8 | **B8+B9** |
|---|---|---|
| 基线 R² | 0.4668 | **0.4829**（+0.0161） |
| ΔR² | +0.0258 | +0.0206 |
| CW t | 12.17 | **10.39** |
| **QLIKE 改善** | 1.1% | **3.6%** |
| **QLIKE DM t** | **0.91** ✗ | **2.97** ✓ |
| **逐日为正** | **49.6%**（抛硬币） | **54.9%** |
| **低估侧 MSE 下降** | **−5.4%** | **+1.8%** |
| 高估侧 MSE 下降 | +12.0% | +5.7% |
| **★严重低估前 10%** | **+1.2%** | **+6.0%**（QLIKE +6.5%） |
| 基线校准（高估占比） | **53.4%** | **51.0%** |

### ★★★ 根因：B8 本身失准，不是「加了特征所以变好」

```
B8 下   53.4% 的点被高估   ← 系统性偏高
B8+B9 下 51.0%              ← 接近对称
```

**一个系统性高估的基线，会让任何「往下修正」的东西看起来一边倒地有用。**
§5.7 测出的「价值全在高估侧、低估侧为负」，主体是**基线偏斜造成的假象**。

> **方法论教训：做分侧诊断之前必须先检查基线的校准。**
> 否则量到的是基线的偏，不是模型的贡献。

### 修正后的结论

在信息完整的基线（B8+B9）之上：

- **ΔR² +0.0206，CW t = 10.39**（140 指数，268 天）
- **两侧均衡**（低估 +1.8% / 高估 +5.7%），不再一边倒
- **在严重低估的尾部最强**（+6.0% / QLIKE +6.5%）—— **这恰好是风控要的性质**
- **QLIKE 显著**（3.6%，t = 2.97，逐日为正 54.9%）

**风控用途成立**，但幅度比最初以为的小一半（+0.0206 vs 最初 B6 口径的 +0.0353）。

### ★ 基线阶梯的每一级都在吃增量，但同时改变它的构成

| 基线升级 | ΔR²（3指数） | 低估侧 | 严重低估前10% |
|---|---|---|---|
| B4 → B6（+滞后收益） | +0.0345 → +0.0353 | — | — |
| B6 → B8（+量/高低价/隔夜/跳跃/日历） | → +0.0292（**−17%**） | −4.6% | +2.5% |
| B8 → B8+B9（+1min 派生） | → +0.0208（**−29%**） | **+3.0%** | **+7.3%** |

**补基线不只是把增量缩小，它还在改变增量的构成** —— 而构成才决定这东西能不能用。

### ⚠️ 「信息集对齐」是递归的，不是一次性检查

B8 已经把 Kronos 的**六列全用上了**（成交额/高低价/隔夜/跳跃），仍然漏掉 B9 ——
**同样六列，换个采样频率再算一遍，又值 +0.0161**。

所以 `baseline-sees-fewer-inputs` 的 probe 必须写成两步：
**① 列出模型输入列并全部建进基线；② 再问一遍这些列里还有什么派生量没做（尤其换频率）。**

## 5.9 ★ 1min 输入判负（用户提出）

这条线变过模型规模、取哪一层、池化、目标视界、基线强度，**唯独输入频率从没变过**。
所以做了一次干净的 A/B：**决策点与目标完全不变**（仍是 5min bar 边界、H 根 5min bar），
唯一变的是喂给 Kronos 的输入粒度。

```
现行:  5min × L=128  -> 回看 640 分钟 = 2.67 个交易日
对照:  1min × L=512  -> 回看 512 分钟 = 2.13 个交易日  (512 = Kronos-small 的 max_context)
```

**四格设计**（★干净的对比是 C vs D：同一基线，只变输入）：

| 格 | Kronos 输入 | 基线 | H=6 ΔR² | H=1 ΔR² |
|---|---|---|---|---|
| A | 5min×128 | B8 | +0.0292 | +0.0081 |
| B | 1min×512 | B8 | +0.0163 | −0.0002 |
| **C** | **1min×512** | **B8+B9** | **+0.0096** | **−0.0006** |
| **D** | **5min×128** | **B8+B9** | **+0.0208** | **+0.0062** |

| | H=6（30min） | H=1（5min） |
|---|---|---|
| **C − D**（输入频率的真实价值） | **−0.0112** | **−0.0068** |
| A − D（B9 吃掉 5min 隐层多少） | +0.0084 | +0.0019 |
| **B − C**（基线没看见的 1min 信息值多少） | **+0.0067** | +0.0004 |

### 判定：1min 输入全面判负，连最短视界都不赢

预注册的判据是 `C−D < −0.003` 即判负，两个视界都远超。
**H=1 上 C 已经跌破零**（−0.0006，CW t 从 10.45 掉到 2.02），
而同视界的 5min 输入仍有 +0.0062（t=10.45）。

原因三条叠加，前两条是结构性的、不随数据量改善：

1. **分词器的量化损失** —— Kronos 把 OHLCV 压成离散 token，1min bar 的价格变动接近
   tick size，被量化掉的比例更大
2. **微观结构噪声** —— Liu-Patton-Sheppard 的「5min 是甜点」对 Kronos 同样成立
3. 回看变短（2.13 vs 2.67 天），受 `max_context=512` 硬约束（这条换更大 context 的模型可缓解）

### ★ 附带：`B − C` 是今天那个坑的直接量化

同样的 1min 隐层，基线没补 B9 时测出 **+0.0163**，补齐后只剩 **+0.0096** ——
**41% 是基线没看见的信息，不是模型的功劳**。

若只跑 B 格就收工，会报出「1min 输入有 +0.0163 增量」，
而真实值是 +0.0096，**再和 5min 输入比还是输**。

## 6. 待补

## 5.9 ★ 1min 输入判负（用户提出）

这条线变过模型规模、取哪一层、池化、目标视界、基线强度，**唯独输入频率从没变过**。
所以做了一次干净的 A/B：**决策点与目标完全不变**（仍是 5min bar 边界、H 根 5min bar），
唯一变的是喂给 Kronos 的输入粒度。

```
现行:  5min × L=128  -> 回看 640 分钟 = 2.67 个交易日
对照:  1min × L=512  -> 回看 512 分钟 = 2.13 个交易日  (512 = Kronos-small 的 max_context)
```

**四格设计**（★干净的对比是 C vs D：同一基线，只变输入）：

| 格 | Kronos 输入 | 基线 | H=6 ΔR² | H=1 ΔR² |
|---|---|---|---|---|
| A | 5min×128 | B8 | +0.0292 | +0.0081 |
| B | 1min×512 | B8 | +0.0163 | −0.0002 |
| **C** | **1min×512** | **B8+B9** | **+0.0096** | **−0.0006** |
| **D** | **5min×128** | **B8+B9** | **+0.0208** | **+0.0062** |

| | H=6（30min） | H=1（5min） |
|---|---|---|
| **C − D**（输入频率的真实价值） | **−0.0112** | **−0.0068** |
| A − D（B9 吃掉 5min 隐层多少） | +0.0084 | +0.0019 |
| **B − C**（基线没看见的 1min 信息值多少） | **+0.0067** | +0.0004 |

### 判定：1min 输入全面判负，连最短视界都不赢

预注册的判据是 `C−D < −0.003` 即判负，两个视界都远超。
**H=1 上 C 已经跌破零**（−0.0006，CW t 从 10.45 掉到 2.02），
而同视界的 5min 输入仍有 +0.0062（t=10.45）。

原因三条叠加，前两条是结构性的、不随数据量改善：

1. **分词器的量化损失** —— Kronos 把 OHLCV 压成离散 token，1min bar 的价格变动接近
   tick size，被量化掉的比例更大
2. **微观结构噪声** —— Liu-Patton-Sheppard 的「5min 是甜点」对 Kronos 同样成立
3. 回看变短（2.13 vs 2.67 天），受 `max_context=512` 硬约束（这条换更大 context 的模型可缓解）

### ★ 附带：`B − C` 是今天那个坑的直接量化

同样的 1min 隐层，基线没补 B9 时测出 **+0.0163**，补齐后只剩 **+0.0096** ——
**41% 是基线没看见的信息，不是模型的功劳**。

若只跑 B 格就收工，会报出「1min 输入有 +0.0163 增量」，
而真实值是 +0.0096，**再和 5min 输入比还是输**。

## 6. 待补

- `test` / `holdout` 一律不开封 —— 本研究在 val 段上闭合，样本外是开放问题

---

## 稳健性：目标构造换一遍，结论不变（2026-09-22 追加）

**威胁**：现行 30min 目标只用 **6 根 5min 收益**估 RV。RV 估计量方差 ∝ 1/n，
6 个收益的测量噪声很大；这个噪声对谁都不可预测，却坐在 R² 的分母里，
于是可能**同时压低基线 R² 并放大表观增量**。
（旁证：同样 30 分钟视界，股指期货用 1800 个 1 秒收益算 RV 时秒级 HAR 基线 R² = 0.836，
而本项目 B8+B9 基线只有 0.493。）

**做法**：除目标外一个字节不改——同一批决策点、同一隐层库（L8n/last）、同一 B8+B9、
同一切分、同样只用 train 拟合且 λ 只在 val 选。判据写在看数之前（val 掉幅 >40% 需改表述）。

| 目标构造 | val 基线 R² | val ΔR² | test 基线 R² | test ΔR² | 可达 R² 上限 |
|---|---|---|---|---|---|
| **A** 6 根 5min 收益（现行，**头条数字**） | 0.4929 | **+0.0148** | 0.5894 | **+0.0127** | 0.769 |
| B 30 根 1min 收益 | 0.7020 | +0.0123 | 0.7459 | +0.0039 | 0.940 |
| **C** 子采样 5min RV（无偏 + 低噪声） | 0.5560 | **+0.0180** | 0.6258 | **+0.0138** | 0.951 |

**结论：威胁排除。** 在正确构造的目标 C 上增量不是缩水而是**更大**（val +21%，test +9%），
CW t 11.13 / 9.58。头条仍报 A（预注册且已发布的口径），C 作为稳健性证据。

### ★中途的一个陷阱（值得单独记）

目标 B 一度显示 test 增量塌 70%（+0.0127 → +0.0039），若直接采信会得出相反结论。
但 **B 本身有偏**：指数是一篮子股票的加权价，成分股不同步成交会把高频指数序列平滑化
（陈旧价格 / Epps 效应）。实测 **1min 指数收益 ρ1 = +0.266，而 5min 上只有 +0.013**。
所以 B 测的是"被平滑过的波动"，不是"更准的波动"。

=> **提高目标的采样频率来降测量噪声，对指数这类合成价格会引入陈旧价格偏差。**
正确做法是保持 5 分钟跨度、把起点错开做子采样（Zhang-Mykland-Aït-Sahalia），即目标 C。
corr(A,C)=0.953、corr(B,C)=0.902 也印证 A 与 C 测的是同一个量。

### 同时确认

test 段 QLIKE 在 C 上是 **−19.9%**（A 上是 −14.8%），仍然为负且更负
=> **「只作预报层、风控/非对称口径不主张」这一判断在更好的目标上更站得住**，
不是原目标噪声造成的假象。

复现：`python src/target_measerr_check.py`

---

## 能不能往更高频走？—— 三条线，全部关闭（2026-09-22）

本项目的配置是 **5min 输入 / 30min 视界**。三个"往高频走"的方向各测了一遍。

### 一、L2 盘口 → 30 秒 / 1 分钟波动（`src/l2_highfreq/`）

中金所 L2 tick（0.5 秒快照 / 5 档，2014-01-02~2025-10-31），IC/IF/IM 主力，
train 段 805 天 / 170 万决策点（每 10 秒一个）。

**先决条件反而过关**：IC 期货**中价**在 1 秒尺度上基本无微观结构噪声——
波动率签名图几乎是平的（0.5s RV / 5min RV = 1.03），收益一阶自相关从 0.5s 的
−0.211 到 1s 的 +0.010。**Liu-Patton-Sheppard「5min 是甜点」是为个股成交价说的，
对股指期货中价不成立。** 所以 30s/60s 的 RV 目标可定义（30/60 个收益）。

**★决定性拆分**：把 L2 特征分成①成交流（成交量/笔数/单笔规模/报价更新强度，
**L1 tick 甚至分钟 K 线就能算**）与②订单簿（价差/深度/队列不平衡/盘口斜率/真 OFI）。

| | 30秒 | 1分钟 | 5分钟 | 15分钟 | 30分钟 |
|---|---|---|---|---|---|
| 秒级 HAR 基线 R² | 0.545 | 0.649 | 0.801 | 0.836 | 0.836 |
| ①成交流 | +0.0069 | +0.0065 | −0.0170 | −0.0222 | −0.0251 |
| **②订单簿（净）** | **+0.0032** | **+0.0032** | **+0.0026** | **+0.0029** | **+0.0026** |

test 段开封（2024-06-11~2025-07-17，269 天 / 76.5 万点，完整 train 拟合）：
订单簿净增量 **+0.0041/+0.0045/+0.0051/+0.0042/+0.0034**，**CW t = 8.93/9.60/7.99/6.43/3.63**。

**★效应是真的，但太小。** 占基线 0.4~0.6%，是本项目隐层（占基线 2~3%）的 1/5；
而且载体只有 3 个期货合约，不是 140 个指数。**结论是"成立但不值得立项"，不是"没有效应"。**

逐组（净效果，加在价格+成交流之上，30min）：
**队列不平衡 QI 在五个视界上全部精确为 ±0.0000**（它是做**方向**预测的主力，对二阶矩零内容）；
盘口斜率 ≈0；深度均值 **−0.0014**；价差均值 +0.0008；真 OFI +0.0016（随视界单调增长，
订单流累积效应确需时间释放）；**唯一像样的是价差的波动 +0.0050**。

★另一个反直觉对照：同一批订单簿特征**只加在价格基线上**时，30min 为 **−0.0091**；
加在"价格+成交流"之上才转为 +0.0026。弱基线缺活跃度变量时，岭回归会征用
价差/QI/深度的**波动**当活跃度代理，而这些代理的水平有制度性漂移，冻结系数一外推就崩。
⇒ **「基线越强增量越少」不是普适的**；当新特征与基线缺失项存在代理关系时，补全基线是在**保护**新特征。

### 二、"往高频走能拿到更多增量"是反的

上表第一行：秒级 HAR 基线 R² 从 30 秒的 0.545 单调升到 30 分钟的 0.836。
**视界越短，波动越持续，最笨的基线吃得越多，留给任何模型的空间越小。**
本项目把主视界定在 30min 是对的。

### 三、ETF 折价溢价（`src/etf_premium_probe.py`，预注册 `docs/PREREG_ETF_PREMIUM.md`）

指数没有订单簿，但跟踪它的 ETF 有，折溢价反映一二级套利压力。
3 对标的：510300↔000300 / 510500↔000905 / 512100↔000852。
折溢价用 **CSMAR ETF L2 TAQ 的真实 IOPV**（`(LastPrice/IOPV−1)×1e4`，覆盖 2015-01~2026-09）。

**★核心设计 gap-ahead（本研究存在的主要理由）**：指数是一篮子加权价，成分股不同步成交
⇒ 指数价格**滞后**（本项目实测 1min 指数收益 ρ1 = **+0.266**，5min 上仅 +0.013）；
ETF 连续交易，**不滞后**。所以 `(ETF价 − 指数价)` 很大程度上是"指数还没走完的那一段"，
而指数**未来**的 RV **机械地包含**这段补涨。故同一特征测三个目标：
gap0 = t→t+30min / gap1 = t+5→t+35min / gap2 = t+10→t+40min。

| 段 | gap0 | gap1 | gap2 | CW t (gap0/1/2) |
|---|---|---|---|---|
| val（决策段） | +0.0022 | **+0.0014** | +0.0003 | 3.10 / **2.53** / 2.05 |
| TEST（开封） | +0.0059 | **+0.0057** | +0.0055 | 3.92 / **3.54** / 3.23 |

**主判据（gap1 净增量 > +0.002 且 CW t > 3）钉在 val 上——未过 ⇒ 判负。**
test 上过了，但那是开封**之后**的观察，**不用于翻案**；否则等于把封存段变成第二个验证段。

支持 val 的三条：val 逐指数不一致（000300 +0.0054 / 000852 **−0.0045** / 000905 +0.0045，
是噪声的形状）；①ETF 自身在 test 上塌到 +0.0000；安慰剂 −0.0001（管线干净，不是 bug）。
支持 test 的一条：驱动**全部来自 pd 的波动**（+0.0051）而非 pd 水平（+0.0007）。

**★这个 val/test 矛盾有干净的裁决路径**：CSMAR 数据覆盖到 2026-09-21，
**holdout（2025-07-18 起）仍然封存** ⇒ 可另开预注册，train+val+test 拟合、holdout 一次性判。
本次不做。

### ★★三条线共同暴露的一个通则

**做波动（二阶矩）预测时，有内容的是「某个量在抖」，不是「某个量的水平」。** 同日三次重复：

| | 水平 | 波动 |
|---|---|---|
| 期货价差 | +0.0008 | **+0.0050** |
| ETF 折溢价 | +0.0007 | **+0.0051** |
| 队列不平衡（纯方向量） | 0.0000 | — |

⇒ 给波动模型选特征，**默认先造每个候选量的滚动标准差，水平项只作对照**。
照搬做方向预测的特征表会系统性选错。

## 7. 复现

```bash
PY=python   # conda env: alphagen (torch CPU + lightgbm)
$PY src/screen_indices.py --workers 80          # universe 筛查 (722 -> 480)
$PY src/extract_layers.py --workers 10 --threads 8   # 10 位置 × 2 池化隐层
$PY src/run_layerscan.py --stage baselines --H 6,12,24
$PY src/run_layerscan.py --stage scan      --H 6,12,24
$PY src/run_layerscan.py --stage combine   --H 6,12,24 --seeds 5
$PY src/run_layerscan.py --stage nulls     --H 6,12,24
$PY src/dm_test_v2.py --H 6                     # DM + Clark-West + QLIKE
# —— 全指数线 ——
$PY src/curate_universe.py --family 深证         # 480 -> 144
$PY src/build_index5m_universe.py --workers 80
$PY src/extract_universe.py --workers 10 --threads 8
$PY src/run_universe_eval.py --H 6,12,24 --pos L8n
# —— 稳健性与旁支(2026-09-22) ——
$PY src/target_measerr_check.py                 # 目标测量误差(6根5min vs 30根1min vs 子采样)
$PY src/build_etf_iopv5m.py --workers 40        # ETF 真实 IOPV 5min 网格
$PY src/etf_premium_probe.py                    # ETF 折溢价 gap-ahead 检验
$PY src/l2_highfreq/build_panel.py              # 中金所 L2 tick -> 决策点面板
$PY src/l2_highfreq/open_test_l2.py             # L2 线 test 开封
```
